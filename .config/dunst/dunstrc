[global]
    font = Arial 11 #Monospace 10

    allow_markup = yes
    plain_text = no

    show_indicators=false
    icon_folders = "/usr/share/icons/Papirus/24x24/actions/:/usr/share/icons/Papirus/24x24/apps/:/usr/share/icons/Papirus/24x24/categories/:/usr/share/icons/Papirus/24x24/devices/:/usr/share/icons/Papirus/24x24/emblems/:/usr/share/icons/Papirus/24x24/emotes/:/usr/share/icons/Papirus/24x24/mimetypes/:/usr/share/icons/Papirus/24x24/places/:/usr/share/icons/Papirus/24x24/status/:/usr/share/icons/Papirus/24x24/panel/"

    min_icon_size=80
    max_icon_size=80
    shrink=false

    format = "<b>%s</b>\n%b"

    sort = yes

    indicate_hidden = yes

    alignment = left
    icon_position = left
    text_icon_padding = 15
    horizontal_padding = 0

    padding = 0
    show_age_threshold = 30

    word_wrap = yes
    ellipsize = end

    ignore_newline = no

    geometry = "450x5-30+20"

    transparency = 40

    idle_threshold = 60

    monitor = 0
    follow = mouse

    # should a notification popped up from history be sticky or
    # timeout as if it would normally do.
    sticky_history = yes

    # The height of a single line. If the height is smaller than the font height,
    # it will get raised to the font height.
    # This adds empty space above and under the text.
    line_height = 2

    # Draw a line of 'separator_height' pixel height between two notifications.
    # Set to 0 to disable
    separator_height = 2

    # Define a color for the separator.
    # possible value are:
    #  * auto: dunst tries to find a color fitting to the background
    #  * foreground: use the same color as the foreground
    #  * frame: use the same color as the frame.
    #  * anything else will be interpreted as a X color
    separator_color = frame

    startup_notification = false

    dmenu = /usr/bin/dmenu -p dunst:
    browser = /usr/bin/firefox -new-tab

[frame]
    width = 0
    color = "#071019"#"#000000"

[shortcuts]
    # shortcuts are specified as [modifier+][modifier+]...key
    # available modifiers are 'ctrl', 'mod1' (the alt-key), 'mod2', 'mod3'
    # and 'mod4' (windows-key)
    # xev might be helpful to find names for keys

    # close notification
    close = ctrl+space

    # close all notifications
    close_all = ctrl+shift+space

    # redisplay last message(s)
    history = ctrl+grave

    # context menu
    context = mod4+shift+i

[urgency_low]
    # IMPORTANT: colors have to be defined in quotation marks.
    # Otherwise the '#' and following  would be interpreted as a comment.
    background = "#071019"
    foreground = "#FFFFFF"
    timeout = 5

[urgency_normal]
    background = "#071019"
    foreground = "#FFFFFF"
    timeout = 5

[urgency_critical]
    background = "#071019"
    foreground = "#FFFFFF"
    timeout = 0


# Every section that isn't one of the above is interpreted as a rules
# to override settings for certain messages.
# Messages can be matched by 'appname', 'summary', 'body' or 'icon'
# and you can override the 'timeout', 'urgency', 'foreground', 'background'
# and 'format'.
# Shell-like globbing will get expanded.
#
# SCRIPTING
# you can specify a script that gets run when the rule matches by setting
# the 'script' option.
# The script will be called as follows:
# script appname summary body icon urgency
# where urgency can be "LOW", "NORMAL" or "CRITICAL".
#
# NOTE: if you don't want a notification to be displayed, set the format to ""
# NOTE: It might be helpful to run dunst -print in a terminal in order to find
# fitting options for rules.

#[volume.sh]
    #script = /home/dstellati/Scripts/./volume.sh up

#[script-test]
#    summary = "*script*"
#    script = dunst_test.sh

#[ignore]
## This notification will not be displayed
#    summary = "foobar"
#    format = ""

#[signed_on]
#    appname = Pidgin
#    summary = "*signed on*"
#    urgency = low

#[signed_off]
#    appname = Pidgin
#    summary = *signed off*
#    urgency = low

#[says]
#    appname = Pidgin
#    summary = *says*
#    urgency = critical
#    format  = "%s "%b""

#[music]
#    appname = Amarok
#    summary = "Now playing"
#    urgency = low

#[twitter]
#    appname = Pidgin
#    summary = *twitter.com*
#    urgency = normal
#
